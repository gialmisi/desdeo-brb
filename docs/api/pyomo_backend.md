# `desdeo_brb.pyomo_backend`

Pyomo-based symbolic model builder. Requires the `pyomo` optional dependency
and an IPOPT binary (`pip install desdeo-brb[pyomo]`).

::: desdeo_brb.pyomo_backend
    options:
      show_root_heading: true
      members:
        - build_pyomo_brb_model
