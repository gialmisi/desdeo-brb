version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_create_environment:
      - pip install uv
    post_install:
      - uv pip install --system -e .[docs,jax]

mkdocs:
  configuration: mkdocs.yml

formats:
  - pdf
  - htmlzip
